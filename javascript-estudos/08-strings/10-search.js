
/**
 * busca um valor na string ou expressão regular, e retona a (posição, index).
 */

let text = "Mr. Blue has a blue house"
let position = text.search("Blue"); //não é case sensitivi result: 4

console.log(position)