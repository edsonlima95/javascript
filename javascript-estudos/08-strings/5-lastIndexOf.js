/**
 *  Retorna o index (posição) da ultima ocorrencia de um valor em uma string
 */
let text = "Hello world, welcome to the word.";

let result2 = text.lastIndexOf("word")

console.log("A palavra começa na posição: ",result2);